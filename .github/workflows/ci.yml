name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  scaffold:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "CI workflow scaffold"
